---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<section class={`rounded-xl border border-secondary-200 bg-white p-5 shadow-card ${className}`}>
  <slot />
</section>
